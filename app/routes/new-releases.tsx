const NewReleases = () => (
  <div>
    <p>
      New Releases Page. Is it okay to render upcoming ones here? Didn't see new-release endpoint on
      TMDB API.
    </p>
  </div>
);

export default NewReleases;
