import { H2, H6 } from '~/components/styles/Text.styles';

export const handle = {
  getSitemapEntries: () => null,
};

const testIndex = () => (
  <>
    <H2>Testing</H2>
    <H6>This route is built for testing purposes.</H6>
  </>
);

export default testIndex;
